[phases.setup]
nixPkgs = ["python311", "gcc", "gfortran", "libgcc"]

[phases.install]
cmds = ["pip install --upgrade pip", "pip install -r requirements.txt"]

[phases.build]
cmds = ["python --version", "pip list"]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT"
